# cURL Installs

This repo contains some bash scripts for running install scripts that use cURL because I'm too lazy to always go find them.

The make file makes them executable and adds them as simlinks to my local binaries for added laziness. This also prevents my system from having bloat.
